# bootstrap.yml
server:
  port: 9002
spring:
  application:
    name: api-gateway
  profiles:
    active: dev
  cloud:
    config:
      uri: http://localhost:9001
      fail-fast: true
management:
  endpoints:
    web:
      exposure:
        include: refresh